kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: plugins/docker
    settings:
      username: "_json_key"
      password: "aaaaa"
      repo: gcr.io/pxlblue/backend
      tags: ${DRONE_COMMIT}
